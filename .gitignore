node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*
.vite/
coverage/
.vercel

# Build / framework caches
.next/
.turbo/
*.tsbuildinfo

# Python
.venv/
**/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Model / training artifacts (live in HF + Modal Volume, never in git)
*.gguf
finetuning/models/
finetuning/gguf/
finetuning/outputs/
*.tar.zst

# demo seed: runtime id cache only (regenerated by seed_demo). The seed/delete
# scripts + DEMO.md ARE committed so the demo survives a new session / PC reset.
demo_cobalt_ids.json
.env*
